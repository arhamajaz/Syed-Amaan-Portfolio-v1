/** @type {import('next').NextConfig} */
const nextConfig = {
    // Ensuring assets like images work correctly in static-like environments or GitHub Pages if needed later
    reactStrictMode: true,
};

export default nextConfig;
